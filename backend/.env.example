# Environment variables (optional)
# Kubernetes config will be loaded from ~/.kube/config by default

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Store defaults
DEFAULT_STORE_SIZE=5Gi
DEFAULT_DB_SIZE=3Gi
STORE_DOMAIN_SUFFIX=.localhost
